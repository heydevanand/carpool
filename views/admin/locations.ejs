<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Locations - PG Carpool</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-car"></i> PG Carpool - Locations
            </a>
            <div>
                <a href="/admin" class="btn btn-outline-light btn-sm me-2">Dashboard</a>
                <a href="/" class="btn btn-outline-light btn-sm">User View</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="fas fa-map-marker-alt"></i> Service Locations</h5>
                    </div>
                    <div class="card-body">
                        <% if (locations.length === 0) { %>
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle"></i> No locations added yet.
                            </div>
                        <% } else { %>
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th>Name</th>
                                            <th>Address</th>
                                            <th>Status</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% locations.forEach(location => { %>
                                            <tr>
                                                <td>
                                                    <strong><%= location.name %></strong>
                                                </td>
                                                <td>
                                                    <%= location.address %>
                                                    <% if (location.coordinates.lat && location.coordinates.lng) { %>
                                                        <br><small class="text-muted">
                                                            <i class="fas fa-map-pin"></i> 
                                                            <%= location.coordinates.lat.toFixed(6) %>, <%= location.coordinates.lng.toFixed(6) %>
                                                        </small>
                                                    <% } %>
                                                </td>
                                                <td>
                                                    <span class="badge bg-<%= location.isActive ? 'success' : 'secondary' %>">
                                                        <%= location.isActive ? 'Active' : 'Inactive' %>
                                                    </span>
                                                </td>
                                                <td>
                                                    <form method="POST" action="/admin/locations/<%= location._id %>/toggle" style="display: inline;">
                                                        <button type="submit" class="btn btn-sm btn-outline-<%= location.isActive ? 'warning' : 'success' %>">
                                                            <i class="fas fa-<%= location.isActive ? 'pause' : 'play' %>"></i>
                                                            <%= location.isActive ? 'Deactivate' : 'Activate' %>
                                                        </button>
                                                    </form>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="fas fa-plus-circle"></i> Add New Location</h5>
                    </div>
                    <div class="card-body">
                        <form method="POST" action="/admin/locations">
                            <div class="mb-3">
                                <label class="form-label">Location Name</label>
                                <input type="text" class="form-control" name="name" required 
                                       placeholder="e.g., Tech Park Gate 1">
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Address</label>
                                <textarea class="form-control" name="address" rows="3" required 
                                          placeholder="Full address of the location"></textarea>
                            </div>
                            <div class="row">
                                <div class="col-6">
                                    <div class="mb-3">
                                        <label class="form-label">Latitude</label>
                                        <input type="number" class="form-control" name="lat" step="any" 
                                               placeholder="12.9716">
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div class="mb-3">
                                        <label class="form-label">Longitude</label>
                                        <input type="number" class="form-control" name="lng" step="any" 
                                               placeholder="77.5946">
                                    </div>
                                </div>
                            </div>
                            <div class="mb-3">
                                <small class="text-muted">
                                    <i class="fas fa-info-circle"></i> 
                                    You can get coordinates from Google Maps by right-clicking on a location.
                                </small>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">
                                <i class="fas fa-plus"></i> Add Location
                            </button>
                        </form>
                    </div>
                </div>

                <div class="card mt-3">
                    <div class="card-header">
                        <h6><i class="fas fa-lightbulb"></i> Quick Tips</h6>
                    </div>
                    <div class="card-body">
                        <ul class="list-unstyled small">
                            <li><i class="fas fa-check text-success"></i> Add major landmarks and gates</li>
                            <li><i class="fas fa-check text-success"></i> Include nearby metro stations</li>
                            <li><i class="fas fa-check text-success"></i> Add office building entrances</li>
                            <li><i class="fas fa-check text-success"></i> Use clear, recognizable names</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
